{
  "name": "evident-project",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix studio && npm run build --prefix studio",
    "start": "npm run start --prefix studio"
  },
  "engines": {
    "node": ">=20"
  }
}
